export declare class PluginFileUtils {
    /** Opens the given file with the default app.
     *
     * Logs if opening the file with the default app failed.
     */
    static openWithDefaultApp(filePath: string): Promise<void>;
}
