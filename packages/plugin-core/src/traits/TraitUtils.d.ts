export declare class TraitUtils {
    /**
     * Shows a warning to user's about needing to enable workspace trust for traits.
     * @returns true if the workspace is trusted.
     */
    static checkWorkspaceTrustAndWarn(): boolean;
}
